version: '3'
services:
  web:
    build:
      context: .
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
      # SSL証明書が必要な場合
      - ./certs:/etc/nginx/certs
